interface:
  display_name: "Agent Workflow"
  short_description: "Route coding work through the bundled harness"
  default_prompt: "Use $agent-workflow to route this coding task through the bundled workflow harness."

policy:
  allow_implicit_invocation: true
